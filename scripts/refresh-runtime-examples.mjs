const examples = [
  'examples/runtime-storyboard/animation.json',
];

console.log('Runtime examples are now hand-maintained so generated refresh scripts do not recreate removed examples.');
console.log(`Kept ${examples.length} maintained runtime example:\n- ${examples.join('\n- ')}`);
