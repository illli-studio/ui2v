{
  "id": "library-timeline",
  "title": "Real Library Timeline",
  "description": "A maintained example where every declared library directly produces motion, geometry, WebGL pixels, physics, or canvas strokes.",
  "name": "Real Library Timeline",
  "mode": "template",
  "duration": 10,
  "fps": 30,
  "resolution": {
    "width": 1920,
    "height": 1080
  },
  "backgroundColor": "#030712",
  "dependencies": [
    "canvas2d",
    "gsap",
    "d3",
    "THREE",
    "Matter",
    "rough"
  ],
  "template": {
    "layers": [
      {
        "id": "stage",
        "name": "Timeline stage",
        "type": "custom-code",
        "zIndex": 0,
        "startTime": 0,
        "endTime": 10,
        "dependencies": [
          "canvas2d"
        ],
        "properties": {
          "dependencies": [
            "canvas2d"
          ],
          "code": "function createRenderer(){\n  function rr(ctx,x,y,w,h,r){ctx.beginPath();ctx.moveTo(x+r,y);ctx.lineTo(x+w-r,y);ctx.quadraticCurveTo(x+w,y,x+w,y+r);ctx.lineTo(x+w,y+h-r);ctx.quadraticCurveTo(x+w,y+h,x+w-r,y+h);ctx.lineTo(x+r,y+h);ctx.quadraticCurveTo(x,y+h,x,y+h-r);ctx.lineTo(x,y+r);ctx.quadraticCurveTo(x,y,x+r,y);ctx.closePath()}\n  return{render(t,context){const ctx=context.mainContext,w=context.width,h=context.height;let g=ctx.createLinearGradient(0,0,w,h);g.addColorStop(0,'#030712');g.addColorStop(.48,'#062033');g.addColorStop(1,'#241326');ctx.fillStyle=g;ctx.fillRect(0,0,w,h);ctx.save();ctx.globalCompositeOperation='screen';[['rgba(125,211,252,.28)',250,220,620],['rgba(123,216,143,.20)',720,820,700],['rgba(250,204,21,.18)',1370,280,560],['rgba(251,113,133,.18)',1660,820,620]].forEach((s,i)=>{const rg=ctx.createRadialGradient(s[1]+Math.sin(t*.32+i)*70,s[2]+Math.cos(t*.36+i)*54,0,s[1],s[2],s[3]);rg.addColorStop(0,s[0]);rg.addColorStop(1,'rgba(0,0,0,0)');ctx.fillStyle=rg;ctx.fillRect(0,0,w,h)});ctx.restore();ctx.save();ctx.globalAlpha=.08;ctx.strokeStyle='#fff';for(let y=130;y<h-90;y+=56){ctx.beginPath();ctx.moveTo(0,y+Math.sin(t+y*.01)*8);ctx.bezierCurveTo(w*.27,y-42,w*.64,y+42,w,y-8);ctx.stroke()}ctx.restore();ctx.fillStyle='rgba(5,12,24,.55)';rr(ctx,76,64,w-152,h-132,34);ctx.fill();ctx.strokeStyle='rgba(255,255,255,.14)';ctx.lineWidth=2;ctx.stroke();ctx.fillStyle='#fff';ctx.font='900 44px system-ui, sans-serif';ctx.fillText('Real Library Timeline',118,130);ctx.fillStyle='rgba(255,255,255,.64)';ctx.font='700 20px system-ui, sans-serif';ctx.fillText('Only libraries that directly generate pixels or motion are listed here.',120,164);const beats=[['GSAP','timeline/easing','#7dd3fc',0],['D3','shape + scale','#7bd88f',2],['THREE','WebGL scene','#facc15',4],['Matter','physics world','#fb7185',6],['rough','hand drawn canvas','#f9a8d4',8]];for(let i=0;i<beats.length;i++){const x=118+i*((w-236)/5);ctx.fillStyle='rgba(255,255,255,.075)';rr(ctx,x,890,(w-286)/5,86,18);ctx.fill();ctx.strokeStyle=t>=beats[i][3]?beats[i][2]:'rgba(255,255,255,.15)';ctx.stroke();ctx.fillStyle=beats[i][2];ctx.font='900 19px system-ui, sans-serif';ctx.fillText('0'+(i+1)+' '+beats[i][0],x+18,922);ctx.fillStyle='rgba(255,255,255,.58)';ctx.font='700 14px system-ui, sans-serif';ctx.fillText(beats[i][1],x+18,950)}ctx.fillStyle='rgba(255,255,255,.14)';rr(ctx,110,h-52,w-220,10,5);ctx.fill();g=ctx.createLinearGradient(110,0,w-110,0);['#7dd3fc','#7bd88f','#facc15','#fb7185','#f9a8d4'].forEach((c,i)=>g.addColorStop(i/4,c));ctx.fillStyle=g;rr(ctx,110,h-52,(w-220)*Math.min(1,t/10),10,5);ctx.fill()}}\n}"
        }
      },
      {
        "id": "beat-gsap",
        "name": "GSAP real timeline",
        "type": "custom-code",
        "zIndex": 1,
        "startTime": 0,
        "endTime": 2,
        "dependencies": [
          "gsap"
        ],
        "properties": {
          "dependencies": [
            "gsap"
          ],
          "code": "function createRenderer(){\n  let tween;\n  function cl(v){return Math.max(0,Math.min(1,v))}\n  return{init(context){const gsap=(context.libs&&context.libs.gsap&&context.libs.gsap.instance)||globalThis.gsap;if(gsap){const state={x:0,rot:-18,glow:0};tween=gsap.timeline({paused:true}).to(state,{x:1,rot:0,glow:1,duration:1,ease:'elastic.out(1,.55)'});this.state=state;this.gsap=true}else{this.state={x:0,rot:-18,glow:0};this.gsap=false}},render(time,context){const ctx=context.mainContext,w=context.width,h=context.height;if(tween)tween.progress(cl(time/2));else{this.state.x=1-Math.pow(1-cl(time/2),4);this.state.rot=(1-this.state.x)*-18;this.state.glow=this.state.x}ctx.fillStyle='#fff';ctx.font='900 42px system-ui, sans-serif';ctx.fillText('01 GSAP drives the motion',118,226);ctx.fillStyle='rgba(189,239,255,.72)';ctx.font='700 22px system-ui, sans-serif';ctx.fillText('A paused gsap.timeline controls position, rotation, and glow before canvas draws.',120,262);ctx.save();ctx.translate(280+this.state.x*1040,h/2+20);ctx.rotate(this.state.rot*Math.PI/180);ctx.shadowColor='#7dd3fc';ctx.shadowBlur=20+45*this.state.glow;ctx.fillStyle='#7dd3fc';ctx.font='900 98px system-ui, sans-serif';ctx.fillText('GSAP',-160,0);ctx.fillStyle='#fff';ctx.font='900 98px system-ui, sans-serif';ctx.fillText('TIMELINE',150,0);ctx.restore();ctx.fillStyle='#7dd3fc';ctx.font='800 20px ui-monospace, Consolas, monospace';ctx.fillText('real gsap.timeline='+(!!this.gsap)+' progress='+(tween?tween.progress().toFixed(2):this.state.x.toFixed(2)),120,812)}}\n}"
        }
      },
      {
        "id": "beat-d3",
        "name": "D3 real chart geometry",
        "type": "custom-code",
        "zIndex": 1,
        "startTime": 2,
        "endTime": 4,
        "dependencies": [
          "d3"
        ],
        "properties": {
          "dependencies": [
            "d3"
          ],
          "code": "function createRenderer(){\n  let line, area, xScale, yScale, color;\n  return{init(context){const d3=(context.libs&&context.libs.d3&&context.libs.d3.instance)||globalThis.d3;this.d3=!!d3;const W=context.width,H=context.height;this.data=Array.from({length:42},(_,i)=>({x:i,y:.45+.32*Math.sin(i*.42)+.16*Math.cos(i*.17)}));if(d3){xScale=d3.scaleLinear().domain([0,41]).range([170,W-170]);yScale=d3.scaleLinear().domain([0,1]).range([740,330]);line=d3.line().x(d=>xScale(d.x)).y(d=>yScale(d.y)).curve(d3.curveCatmullRom.alpha(.65));area=d3.area().x(d=>xScale(d.x)).y0(760).y1(d=>yScale(d.y)).curve(d3.curveCatmullRom.alpha(.65));color=d3.scaleSequential(d3.interpolateTurbo).domain([0,41])}},render(time,context){const ctx=context.mainContext,w=context.width;ctx.fillStyle='#fff';ctx.font='900 42px system-ui, sans-serif';ctx.fillText('02 D3 generates the chart path',118,226);ctx.fillStyle='rgba(189,239,255,.72)';ctx.font='700 22px system-ui, sans-serif';ctx.fillText('d3.scaleLinear + d3.line + d3.area produce the actual path geometry.',120,262);const p=Math.min(1,time/2);ctx.save();ctx.lineWidth=2;ctx.strokeStyle='rgba(255,255,255,.14)';for(let y=340;y<=760;y+=70){ctx.beginPath();ctx.moveTo(160,y);ctx.lineTo(w-160,y);ctx.stroke()}if(this.d3&&line&&area){const visible=this.data.slice(0,Math.max(2,Math.floor(this.data.length*p)));let path=new Path2D(area(visible));ctx.fillStyle='rgba(123,216,143,.18)';ctx.fill(path);path=new Path2D(line(visible));ctx.strokeStyle='#7bd88f';ctx.lineWidth=8;ctx.shadowColor='#7bd88f';ctx.shadowBlur=22;ctx.stroke(path);for(const d of visible){ctx.fillStyle=color(d.x);ctx.beginPath();ctx.arc(xScale(d.x),yScale(d.y),7,0,Math.PI*2);ctx.fill()}}ctx.restore();ctx.fillStyle='#7bd88f';ctx.font='800 20px ui-monospace, Consolas, monospace';ctx.fillText('real d3 path='+(!!this.d3)+' points='+Math.floor(this.data.length*p),120,812)}}\n}"
        }
      },
      {
        "id": "beat-three",
        "name": "THREE real WebGL render",
        "type": "custom-code",
        "zIndex": 1,
        "startTime": 4,
        "endTime": 6,
        "dependencies": [
          "THREE"
        ],
        "properties": {
          "dependencies": [
            "THREE"
          ],
          "code": "function createRenderer(){\n  let renderer, scene, camera, cube, canvas;\n  return{init(context){const THREE=(context.libs&&context.libs.THREE&&context.libs.THREE.instance)||globalThis.THREE;this.THREE=!!THREE;if(!THREE)return;canvas=document.createElement('canvas');canvas.width=900;canvas.height=520;renderer=new THREE.WebGLRenderer({canvas,alpha:true,antialias:true,preserveDrawingBuffer:true});renderer.setSize(900,520,false);renderer.setPixelRatio(1);scene=new THREE.Scene();camera=new THREE.PerspectiveCamera(45,900/520,.1,100);camera.position.z=4.2;const geo=new THREE.BoxGeometry(1.55,1.55,1.55);const mat=new THREE.MeshNormalMaterial();cube=new THREE.Mesh(geo,mat);scene.add(cube);const wire=new THREE.LineSegments(new THREE.EdgesGeometry(geo),new THREE.LineBasicMaterial({color:0xffffff,transparent:true,opacity:.55}));cube.add(wire)},render(time,context){const ctx=context.mainContext,w=context.width,h=context.height;ctx.fillStyle='#fff';ctx.font='900 42px system-ui, sans-serif';ctx.fillText('03 THREE renders a real WebGL scene',118,226);ctx.fillStyle='rgba(189,239,255,.72)';ctx.font='700 22px system-ui, sans-serif';ctx.fillText('THREE.WebGLRenderer draws to an offscreen canvas, then that canvas is composited here.',120,262);if(renderer&&cube){cube.rotation.x=time*1.2;cube.rotation.y=time*1.55;renderer.render(scene,camera);ctx.save();ctx.shadowColor='#facc15';ctx.shadowBlur=34;ctx.drawImage(canvas,(w-900)/2,310,900,520);ctx.restore()}ctx.fillStyle='#facc15';ctx.font='800 20px ui-monospace, Consolas, monospace';ctx.fillText('real THREE.WebGLRenderer='+(!!renderer),120,812)}}\n}"
        }
      },
      {
        "id": "beat-matter",
        "name": "Matter real physics world",
        "type": "custom-code",
        "zIndex": 1,
        "startTime": 6,
        "endTime": 8,
        "dependencies": [
          "Matter"
        ],
        "properties": {
          "dependencies": [
            "Matter"
          ],
          "code": "function createRenderer(){\n  let engine, bodies=[];\n  return{init(context){const Matter=(context.libs&&context.libs.Matter&&context.libs.Matter.instance)||globalThis.Matter;this.Matter=!!Matter;if(!Matter)return;engine=Matter.Engine.create();engine.gravity.y=.9;const ground=Matter.Bodies.rectangle(960,780,1320,38,{isStatic:true,angle:-.04});bodies=Array.from({length:15},(_,i)=>Matter.Bodies.circle(360+(i%5)*210,260+Math.floor(i/5)*72,28+(i%4)*7,{restitution:.82,friction:.02}));Matter.World.add(engine.world,[ground,...bodies])},render(time,context){const ctx=context.mainContext,w=context.width;ctx.fillStyle='#fff';ctx.font='900 42px system-ui, sans-serif';ctx.fillText('04 Matter runs the physics world',118,226);ctx.fillStyle='rgba(189,239,255,.72)';ctx.font='700 22px system-ui, sans-serif';ctx.fillText('Matter.Engine.update advances real rigid bodies; canvas only draws their positions.',120,262);const Matter=(context.libs&&context.libs.Matter&&context.libs.Matter.instance)||globalThis.Matter;if(engine&&Matter){Matter.Engine.update(engine,1000/60);ctx.strokeStyle='rgba(255,255,255,.25)';ctx.lineWidth=5;ctx.beginPath();ctx.moveTo(300,810);ctx.lineTo(1620,755);ctx.stroke();for(let i=0;i<bodies.length;i++){const b=bodies[i];ctx.fillStyle=i%3===0?'#fb7185':i%3===1?'#7dd3fc':'#7bd88f';ctx.shadowColor=ctx.fillStyle;ctx.shadowBlur=18;ctx.beginPath();ctx.arc(b.position.x,b.position.y,b.circleRadius||32,0,Math.PI*2);ctx.fill();ctx.shadowBlur=0}}ctx.fillStyle='#fb7185';ctx.font='800 20px ui-monospace, Consolas, monospace';ctx.fillText('real Matter.Engine='+(!!engine)+' bodies='+bodies.length,120,812)}}\n}"
        }
      },
      {
        "id": "beat-rough",
        "name": "rough real canvas strokes",
        "type": "custom-code",
        "zIndex": 1,
        "startTime": 8,
        "endTime": 10,
        "dependencies": [
          "rough"
        ],
        "properties": {
          "dependencies": [
            "rough"
          ],
          "code": "function createRenderer(){\n  let rc;\n  return{init(context){const rough=(context.libs&&context.libs.rough&&context.libs.rough.instance)||globalThis.rough;this.rough=!!rough;if(rough&&rough.canvas)rc=rough.canvas(context.mainCanvas)},render(time,context){const ctx=context.mainContext,w=context.width,h=context.height;ctx.fillStyle='#fff';ctx.font='900 42px system-ui, sans-serif';ctx.fillText('05 rough draws directly on canvas',118,226);ctx.fillStyle='rgba(189,239,255,.72)';ctx.font='700 22px system-ui, sans-serif';ctx.fillText('rough.canvas(mainCanvas) produces the hand-drawn rectangles, arcs, and curves.',120,262);if(rc){for(let i=0;i<8;i++){const x=260+i*178,y=420+Math.sin(time*2+i)*55;rc.rectangle(x,y,118,118,{stroke:i%2?'#f9a8d4':'#7dd3fc',strokeWidth:3,roughness:2.4,fill:i%2?'rgba(249,168,212,.18)':'rgba(125,211,252,.18)',fillStyle:'hachure'});rc.circle(x+59,y+59,58+Math.sin(time+i)*18,{stroke:'#facc15',strokeWidth:2.5,roughness:2.2});}rc.curve([[240,720],[520,650+Math.sin(time)*80],[900,740],[1280,620+Math.cos(time)*80],[1660,720]],{stroke:'#7bd88f',strokeWidth:5,roughness:1.5})}ctx.fillStyle='#f9a8d4';ctx.font='800 20px ui-monospace, Consolas, monospace';ctx.fillText('real rough.canvas='+(!!rc),120,812)}}\n}"
        }
      }
    ]
  }
}
