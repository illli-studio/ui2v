{
  "id": "paper-route",
  "title": "Paper Route Planner",
  "description": "A delivery route story rendered by real Paper.js paths.",
  "name": "Paper Route Planner",
  "mode": "template",
  "duration": 8,
  "fps": 30,
  "resolution": {
    "width": 1920,
    "height": 1080
  },
  "backgroundColor": "#051018",
  "dependencies": [
    "paper"
  ],
  "template": {
    "layers": [
      {
        "id": "paper-scene",
        "name": "Paper route story",
        "type": "custom-code",
        "zIndex": 0,
        "startTime": 0,
        "endTime": 8,
        "dependencies": [
          "paper"
        ],
        "properties": {
          "dependencies": [
            "paper"
          ],
          "code": "function createRenderer(){\n  let paperScope, canvas, route, comet, checkpoints=[];\n  return{init(context){const paperLib=(context.libs&&context.libs.paper&&context.libs.paper.instance)||globalThis.paper;if(!paperLib)return;canvas=document.createElement('canvas');canvas.width=context.width;canvas.height=context.height;paperScope=new paperLib.PaperScope();paperScope.setup(canvas);const p=paperScope;route=new p.Path({strokeColor:'#7dd3fc',strokeWidth:9,strokeCap:'round'});[[180,760],[410,600],[690,690],[940,420],[1230,500],[1540,300],[1740,410]].forEach(pt=>route.add(new p.Point(pt[0],pt[1])));route.smooth({type:'catmull-rom',factor:.6});comet=new p.Path.Circle(new p.Point(180,760),22);comet.fillColor='#facc15';for(const pt of route.segments){const c=new p.Path.Circle(pt.point,12);c.strokeColor='#7bd88f';c.strokeWidth=4;checkpoints.push(c)}},render(t,context){const ctx=context.mainContext,w=context.width,h=context.height;ctx.fillStyle='#051018';ctx.fillRect(0,0,w,h);ctx.fillStyle='#fff';ctx.font='900 54px system-ui,sans-serif';ctx.fillText('Paper Route Planner',96,112);ctx.fillStyle='rgba(255,255,255,.66)';ctx.font='700 24px system-ui,sans-serif';ctx.fillText('Paper.js owns the smooth route path, checkpoints, and moving courier dot.',100,152);if(paperScope&&route){const p=paperScope;const progress=Math.min(1,t/8);route.dashArray=[route.length*progress,route.length];route.dashOffset=0;const point=route.getPointAt(route.length*progress);if(point)comet.position=point;for(let i=0;i<checkpoints.length;i++){checkpoints[i].fillColor=i/Math.max(1,checkpoints.length-1)<=progress?'#7bd88f':'#0b1b24'}p.view.update();ctx.drawImage(canvas,0,0,w,h)}ctx.fillStyle='#7bd88f';ctx.font='800 22px ui-monospace,Consolas,monospace';ctx.fillText('real paper.Path.smooth + getPointAt animate the route',100,820)}}\n}"
        }
      }
    ]
  }
}
