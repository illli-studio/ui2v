{
  "schema": "uiv-runtime",
  "id": "uiv-commerce-command-center",
  "name": "AI Commerce Command Center",
  "version": "0.5.0",
  "duration": 16,
  "fps": 30,
  "resolution": {
    "width": 1920,
    "height": 1080
  },
  "backgroundColor": "#030712",
  "variables": {
    "accent": "#7BD88F"
  },
  "dependencies": [
    "canvas2d"
  ],
  "markers": [],
  "timeline": {
    "segments": [
      {
        "id": "hero",
        "label": "Hero",
        "startTime": 0,
        "endTime": 4,
        "dependencies": [
          "canvas2d"
        ],
        "code": "\nfunction clamp(value) { return Math.max(0, Math.min(1, value)); }\nfunction easeOut(value) { value = clamp(value); return 1 - Math.pow(1 - value, 3); }\nfunction pulse(t, speed, phase) { return 0.5 + 0.5 * Math.sin(t * speed + phase); }\nfunction rr(ctx, x, y, w, h, r) {\n  ctx.beginPath(); ctx.moveTo(x + r, y); ctx.lineTo(x + w - r, y); ctx.quadraticCurveTo(x + w, y, x + w, y + r); ctx.lineTo(x + w, y + h - r); ctx.quadraticCurveTo(x + w, y + h, x + w - r, y + h); ctx.lineTo(x + r, y + h); ctx.quadraticCurveTo(x, y + h, x, y + h - r); ctx.lineTo(x, y + r); ctx.quadraticCurveTo(x, y, x + r, y); ctx.closePath();\n}\nfunction background(ctx, w, h, t) {\n  const bg = ctx.createLinearGradient(0, 0, w, h); bg.addColorStop(0, '#030712'); bg.addColorStop(0.46, '#071A2F'); bg.addColorStop(1, '#0F2418'); ctx.fillStyle = bg; ctx.fillRect(0, 0, w, h);\n  ctx.save(); ctx.globalCompositeOperation = 'screen'; ctx.globalAlpha = 0.24;\n  let g = ctx.createRadialGradient(w * 0.2, h * 0.16, 20, w * 0.2, h * 0.16, 560); g.addColorStop(0, '#00D4FF'); g.addColorStop(1, 'rgba(0,212,255,0)'); ctx.fillStyle = g; ctx.fillRect(0, 0, w, h);\n  g = ctx.createRadialGradient(w * 0.84, h * 0.72, 20, w * 0.84, h * 0.72, 620); g.addColorStop(0, '#7BD88F'); g.addColorStop(1, 'rgba(123,216,143,0)'); ctx.fillStyle = g; ctx.fillRect(0, 0, w, h); ctx.restore();\n  ctx.save(); ctx.globalAlpha = 0.09; ctx.strokeStyle = '#DFF6FF'; for (let x = -180; x < w + 180; x += 88) { ctx.beginPath(); ctx.moveTo(x + (t * 20) % 88, 0); ctx.lineTo(x - 320 + (t * 20) % 88, h); ctx.stroke(); } ctx.restore();\n}\nfunction hud(ctx, w, h, t, label) {\n  ctx.save(); ctx.fillStyle = 'rgba(0,0,0,.38)'; rr(ctx, 48, 46, 470, 108, 18); ctx.fill(); ctx.strokeStyle = 'rgba(255,255,255,.14)'; ctx.stroke(); ctx.fillStyle = '#DFF6FF'; ctx.font = '18px monospace'; ctx.fillText('commerce command center', 72, 84); ctx.fillStyle = '#7BD88F'; ctx.fillText(label + ' ? frame ' + Math.floor(t * 30), 72, 118); ctx.restore();\n}\nfunction progress(ctx, w, h, t, d) { ctx.fillStyle = 'rgba(255,255,255,.14)'; rr(ctx, 120, h - 74, w - 240, 12, 6); ctx.fill(); const g = ctx.createLinearGradient(120, 0, w - 120, 0); g.addColorStop(0, '#00D4FF'); g.addColorStop(.55, '#7BD88F'); g.addColorStop(1, '#F2AA4C'); ctx.fillStyle = g; rr(ctx, 120, h - 74, (w - 240) * clamp(t / 16), 12, 6); ctx.fill(); }\n\nfunction render(t, context) {\n  const p = easeOut(context.progress ?? 0); const ctx = context.ctx; const w = context.width; const h = context.height; background(ctx, w, h, t);\n  ctx.save(); ctx.globalAlpha = p; ctx.textAlign = 'center'; ctx.shadowColor = '#7BD88F'; ctx.shadowBlur = 34; ctx.fillStyle = '#FFFFFF'; ctx.font = '900 88px Arial, sans-serif'; ctx.fillText('AI Commerce Command Center', w / 2, 338 + (1 - p) * 42); ctx.shadowBlur = 0; ctx.fillStyle = '#BDEFFF'; ctx.font = '30px Arial, sans-serif'; ctx.fillText('orders, revenue, inventory, and risk signals in one generated video', w / 2, 402);\n  const chips = ['Live GMV', 'Risk Radar', 'Inventory AI', 'Dispatch Ops']; for (let i = 0; i < chips.length; i++) { const x = w / 2 - 420 + i * 280; ctx.fillStyle = 'rgba(255,255,255,.08)'; rr(ctx, x - 102, 504, 204, 66, 18); ctx.fill(); ctx.strokeStyle = i % 2 ? '#F2AA4C' : '#00D4FF'; ctx.stroke(); ctx.fillStyle = '#F6FBFF'; ctx.font = '800 18px Arial, sans-serif'; ctx.fillText(chips[i], x, 545); }\n  for (let i = 0; i < 18; i++) { const a = t * .7 + i * Math.PI * 2 / 18; ctx.strokeStyle = i % 2 ? 'rgba(242,170,76,.32)' : 'rgba(0,212,255,.28)'; ctx.beginPath(); ctx.ellipse(w / 2, 716, 210 + i * 16, 44 + i * 2, Math.sin(a) * .32, 0, Math.PI * 2); ctx.stroke(); }\n  ctx.restore(); hud(ctx, w, h, t, 'hero overview'); progress(ctx, w, h, t, 16);\n}"
      },
      {
        "id": "live",
        "label": "Live Revenue",
        "startTime": 4,
        "endTime": 8,
        "dependencies": [
          "canvas2d"
        ],
        "code": "\nfunction clamp(value) { return Math.max(0, Math.min(1, value)); }\nfunction easeOut(value) { value = clamp(value); return 1 - Math.pow(1 - value, 3); }\nfunction pulse(t, speed, phase) { return 0.5 + 0.5 * Math.sin(t * speed + phase); }\nfunction rr(ctx, x, y, w, h, r) {\n  ctx.beginPath(); ctx.moveTo(x + r, y); ctx.lineTo(x + w - r, y); ctx.quadraticCurveTo(x + w, y, x + w, y + r); ctx.lineTo(x + w, y + h - r); ctx.quadraticCurveTo(x + w, y + h, x + w - r, y + h); ctx.lineTo(x + r, y + h); ctx.quadraticCurveTo(x, y + h, x, y + h - r); ctx.lineTo(x, y + r); ctx.quadraticCurveTo(x, y, x + r, y); ctx.closePath();\n}\nfunction background(ctx, w, h, t) {\n  const bg = ctx.createLinearGradient(0, 0, w, h); bg.addColorStop(0, '#030712'); bg.addColorStop(0.46, '#071A2F'); bg.addColorStop(1, '#0F2418'); ctx.fillStyle = bg; ctx.fillRect(0, 0, w, h);\n  ctx.save(); ctx.globalCompositeOperation = 'screen'; ctx.globalAlpha = 0.24;\n  let g = ctx.createRadialGradient(w * 0.2, h * 0.16, 20, w * 0.2, h * 0.16, 560); g.addColorStop(0, '#00D4FF'); g.addColorStop(1, 'rgba(0,212,255,0)'); ctx.fillStyle = g; ctx.fillRect(0, 0, w, h);\n  g = ctx.createRadialGradient(w * 0.84, h * 0.72, 20, w * 0.84, h * 0.72, 620); g.addColorStop(0, '#7BD88F'); g.addColorStop(1, 'rgba(123,216,143,0)'); ctx.fillStyle = g; ctx.fillRect(0, 0, w, h); ctx.restore();\n  ctx.save(); ctx.globalAlpha = 0.09; ctx.strokeStyle = '#DFF6FF'; for (let x = -180; x < w + 180; x += 88) { ctx.beginPath(); ctx.moveTo(x + (t * 20) % 88, 0); ctx.lineTo(x - 320 + (t * 20) % 88, h); ctx.stroke(); } ctx.restore();\n}\nfunction hud(ctx, w, h, t, label) {\n  ctx.save(); ctx.fillStyle = 'rgba(0,0,0,.38)'; rr(ctx, 48, 46, 470, 108, 18); ctx.fill(); ctx.strokeStyle = 'rgba(255,255,255,.14)'; ctx.stroke(); ctx.fillStyle = '#DFF6FF'; ctx.font = '18px monospace'; ctx.fillText('commerce command center', 72, 84); ctx.fillStyle = '#7BD88F'; ctx.fillText(label + ' ? frame ' + Math.floor(t * 30), 72, 118); ctx.restore();\n}\nfunction progress(ctx, w, h, t, d) { ctx.fillStyle = 'rgba(255,255,255,.14)'; rr(ctx, 120, h - 74, w - 240, 12, 6); ctx.fill(); const g = ctx.createLinearGradient(120, 0, w - 120, 0); g.addColorStop(0, '#00D4FF'); g.addColorStop(.55, '#7BD88F'); g.addColorStop(1, '#F2AA4C'); ctx.fillStyle = g; rr(ctx, 120, h - 74, (w - 240) * clamp(t / 16), 12, 6); ctx.fill(); }\n\nfunction render(t, context) {\n  const p = easeOut(context.progress ?? 0); const ctx = context.ctx; const w = context.width; const h = context.height; background(ctx, w, h, t);\n  ctx.save(); ctx.globalAlpha = p; ctx.fillStyle = '#FFFFFF'; ctx.font = '900 60px Arial, sans-serif'; ctx.fillText('Live Revenue Pulse', 120, 150); ctx.fillStyle = '#A9BDD2'; ctx.font = '24px Arial, sans-serif'; ctx.fillText('Every bar, sparkline, and KPI is deterministic Canvas output.', 124, 200);\n  ctx.fillStyle = 'rgba(3,7,18,.56)'; rr(ctx, 110, 270, 1120, 590, 34); ctx.fill(); ctx.strokeStyle = 'rgba(255,255,255,.16)'; ctx.stroke();\n  const base = 780; for (let i = 0; i < 38; i++) { const x = 152 + i * 27; const height = (110 + Math.sin(t * 1.6 + i * .43) * 64 + (i % 9) * 34) * p; const color = i % 3 === 0 ? '#00D4FF' : i % 3 === 1 ? '#7BD88F' : '#F2AA4C'; const g = ctx.createLinearGradient(x, base - height, x, base); g.addColorStop(0, color); g.addColorStop(1, 'rgba(255,255,255,.08)'); ctx.fillStyle = g; rr(ctx, x, base - height, 16, height, 8); ctx.fill(); }\n  ctx.strokeStyle = '#FFFFFF'; ctx.globalAlpha = .76 * p; ctx.lineWidth = 4; ctx.beginPath(); for (let k = 0; k < 160; k++) { const x = 150 + k * 960 / 159; const y = 435 + Math.sin(k * .13 + t * 2) * 50 + Math.cos(k * .04 + t) * 74; if (k === 0) ctx.moveTo(x, y); else ctx.lineTo(x, y); } ctx.stroke(); ctx.globalAlpha = p;\n  const cards = [['GMV today', '$842K', '#7BD88F'], ['Checkout lift', '+18.4%', '#00D4FF'], ['Refund risk', '-9.2%', '#F2AA4C']]; for (let i = 0; i < cards.length; i++) { const y = 300 + i * 170; ctx.fillStyle = 'rgba(255,255,255,.08)'; rr(ctx, 1290, y, 480, 132, 26); ctx.fill(); ctx.strokeStyle = cards[i][2]; ctx.stroke(); ctx.fillStyle = cards[i][2]; ctx.beginPath(); ctx.arc(1334, y + 44, 11 + pulse(t, 4, i) * 4, 0, Math.PI * 2); ctx.fill(); ctx.fillStyle = '#FFFFFF'; ctx.font = '900 34px Arial, sans-serif'; ctx.fillText(cards[i][1], 1370, y + 56); ctx.fillStyle = '#A9BDD2'; ctx.font = '22px Arial, sans-serif'; ctx.fillText(cards[i][0], 1370, y + 96); }\n  ctx.restore(); hud(ctx, w, h, t, 'live revenue'); progress(ctx, w, h, t, 16);\n}"
      },
      {
        "id": "ops",
        "label": "Ops Workflow",
        "startTime": 8,
        "endTime": 12,
        "dependencies": [
          "canvas2d"
        ],
        "code": "\nfunction clamp(value) { return Math.max(0, Math.min(1, value)); }\nfunction easeOut(value) { value = clamp(value); return 1 - Math.pow(1 - value, 3); }\nfunction pulse(t, speed, phase) { return 0.5 + 0.5 * Math.sin(t * speed + phase); }\nfunction rr(ctx, x, y, w, h, r) {\n  ctx.beginPath(); ctx.moveTo(x + r, y); ctx.lineTo(x + w - r, y); ctx.quadraticCurveTo(x + w, y, x + w, y + r); ctx.lineTo(x + w, y + h - r); ctx.quadraticCurveTo(x + w, y + h, x + w - r, y + h); ctx.lineTo(x + r, y + h); ctx.quadraticCurveTo(x, y + h, x, y + h - r); ctx.lineTo(x, y + r); ctx.quadraticCurveTo(x, y, x + r, y); ctx.closePath();\n}\nfunction background(ctx, w, h, t) {\n  const bg = ctx.createLinearGradient(0, 0, w, h); bg.addColorStop(0, '#030712'); bg.addColorStop(0.46, '#071A2F'); bg.addColorStop(1, '#0F2418'); ctx.fillStyle = bg; ctx.fillRect(0, 0, w, h);\n  ctx.save(); ctx.globalCompositeOperation = 'screen'; ctx.globalAlpha = 0.24;\n  let g = ctx.createRadialGradient(w * 0.2, h * 0.16, 20, w * 0.2, h * 0.16, 560); g.addColorStop(0, '#00D4FF'); g.addColorStop(1, 'rgba(0,212,255,0)'); ctx.fillStyle = g; ctx.fillRect(0, 0, w, h);\n  g = ctx.createRadialGradient(w * 0.84, h * 0.72, 20, w * 0.84, h * 0.72, 620); g.addColorStop(0, '#7BD88F'); g.addColorStop(1, 'rgba(123,216,143,0)'); ctx.fillStyle = g; ctx.fillRect(0, 0, w, h); ctx.restore();\n  ctx.save(); ctx.globalAlpha = 0.09; ctx.strokeStyle = '#DFF6FF'; for (let x = -180; x < w + 180; x += 88) { ctx.beginPath(); ctx.moveTo(x + (t * 20) % 88, 0); ctx.lineTo(x - 320 + (t * 20) % 88, h); ctx.stroke(); } ctx.restore();\n}\nfunction hud(ctx, w, h, t, label) {\n  ctx.save(); ctx.fillStyle = 'rgba(0,0,0,.38)'; rr(ctx, 48, 46, 470, 108, 18); ctx.fill(); ctx.strokeStyle = 'rgba(255,255,255,.14)'; ctx.stroke(); ctx.fillStyle = '#DFF6FF'; ctx.font = '18px monospace'; ctx.fillText('commerce command center', 72, 84); ctx.fillStyle = '#7BD88F'; ctx.fillText(label + ' ? frame ' + Math.floor(t * 30), 72, 118); ctx.restore();\n}\nfunction progress(ctx, w, h, t, d) { ctx.fillStyle = 'rgba(255,255,255,.14)'; rr(ctx, 120, h - 74, w - 240, 12, 6); ctx.fill(); const g = ctx.createLinearGradient(120, 0, w - 120, 0); g.addColorStop(0, '#00D4FF'); g.addColorStop(.55, '#7BD88F'); g.addColorStop(1, '#F2AA4C'); ctx.fillStyle = g; rr(ctx, 120, h - 74, (w - 240) * clamp(t / 16), 12, 6); ctx.fill(); }\n\nfunction render(t, context) {\n  const p = easeOut(context.progress ?? 0); const ctx = context.ctx; const w = context.width; const h = context.height; background(ctx, w, h, t);\n  ctx.save(); ctx.globalAlpha = p; ctx.textAlign = 'left'; ctx.fillStyle = '#FFFFFF'; ctx.font = '900 60px Arial, sans-serif'; ctx.fillText('AI Ops Workflow', 120, 150); ctx.fillStyle = '#A9BDD2'; ctx.font = '24px Arial, sans-serif'; ctx.fillText('Orders route through risk, inventory, fulfillment, and support signals.', 124, 200);\n  const lanes = [['Orders', '#00D4FF'], ['Risk', '#F2AA4C'], ['Inventory', '#7BD88F'], ['Dispatch', '#A78BFA'], ['Support', '#00D4FF']]; for (let i = 0; i < lanes.length; i++) { const x = 170 + i * 340; const y = 530 + Math.sin(t + i) * 8; ctx.fillStyle = 'rgba(255,255,255,.08)'; rr(ctx, x - 116, y - 58, 232, 116, 24); ctx.fill(); ctx.strokeStyle = lanes[i][1]; ctx.lineWidth = 2; ctx.stroke(); ctx.fillStyle = '#F6FBFF'; ctx.font = '900 27px Arial, sans-serif'; ctx.textAlign = 'center'; ctx.fillText(lanes[i][0], x, y + 8); if (i < lanes.length - 1) { ctx.strokeStyle = 'rgba(255,255,255,.32)'; ctx.beginPath(); ctx.moveTo(x + 124, y); ctx.lineTo(x + 216, y); ctx.stroke(); ctx.fillStyle = '#7BD88F'; ctx.beginPath(); ctx.arc(x + 124 + ((t * 80 + i * 30) % 92), y, 5, 0, Math.PI * 2); ctx.fill(); } }\n  for (let i = 0; i < 28; i++) { const x = 160 + (i * 61 + t * 42) % 1580; const y = 760 + Math.sin(t * 1.7 + i) * 42; ctx.fillStyle = i % 4 === 0 ? '#F2AA4C' : '#00D4FF'; ctx.globalAlpha = .35 * p; ctx.beginPath(); ctx.arc(x, y, 2 + i % 4, 0, Math.PI * 2); ctx.fill(); }\n  ctx.restore(); hud(ctx, w, h, t, 'ops workflow'); progress(ctx, w, h, t, 16);\n}"
      },
      {
        "id": "final",
        "label": "Final Report",
        "startTime": 12,
        "endTime": 16,
        "dependencies": [
          "canvas2d"
        ],
        "code": "\nfunction clamp(value) { return Math.max(0, Math.min(1, value)); }\nfunction easeOut(value) { value = clamp(value); return 1 - Math.pow(1 - value, 3); }\nfunction pulse(t, speed, phase) { return 0.5 + 0.5 * Math.sin(t * speed + phase); }\nfunction rr(ctx, x, y, w, h, r) {\n  ctx.beginPath(); ctx.moveTo(x + r, y); ctx.lineTo(x + w - r, y); ctx.quadraticCurveTo(x + w, y, x + w, y + r); ctx.lineTo(x + w, y + h - r); ctx.quadraticCurveTo(x + w, y + h, x + w - r, y + h); ctx.lineTo(x + r, y + h); ctx.quadraticCurveTo(x, y + h, x, y + h - r); ctx.lineTo(x, y + r); ctx.quadraticCurveTo(x, y, x + r, y); ctx.closePath();\n}\nfunction background(ctx, w, h, t) {\n  const bg = ctx.createLinearGradient(0, 0, w, h); bg.addColorStop(0, '#030712'); bg.addColorStop(0.46, '#071A2F'); bg.addColorStop(1, '#0F2418'); ctx.fillStyle = bg; ctx.fillRect(0, 0, w, h);\n  ctx.save(); ctx.globalCompositeOperation = 'screen'; ctx.globalAlpha = 0.24;\n  let g = ctx.createRadialGradient(w * 0.2, h * 0.16, 20, w * 0.2, h * 0.16, 560); g.addColorStop(0, '#00D4FF'); g.addColorStop(1, 'rgba(0,212,255,0)'); ctx.fillStyle = g; ctx.fillRect(0, 0, w, h);\n  g = ctx.createRadialGradient(w * 0.84, h * 0.72, 20, w * 0.84, h * 0.72, 620); g.addColorStop(0, '#7BD88F'); g.addColorStop(1, 'rgba(123,216,143,0)'); ctx.fillStyle = g; ctx.fillRect(0, 0, w, h); ctx.restore();\n  ctx.save(); ctx.globalAlpha = 0.09; ctx.strokeStyle = '#DFF6FF'; for (let x = -180; x < w + 180; x += 88) { ctx.beginPath(); ctx.moveTo(x + (t * 20) % 88, 0); ctx.lineTo(x - 320 + (t * 20) % 88, h); ctx.stroke(); } ctx.restore();\n}\nfunction hud(ctx, w, h, t, label) {\n  ctx.save(); ctx.fillStyle = 'rgba(0,0,0,.38)'; rr(ctx, 48, 46, 470, 108, 18); ctx.fill(); ctx.strokeStyle = 'rgba(255,255,255,.14)'; ctx.stroke(); ctx.fillStyle = '#DFF6FF'; ctx.font = '18px monospace'; ctx.fillText('commerce command center', 72, 84); ctx.fillStyle = '#7BD88F'; ctx.fillText(label + ' ? frame ' + Math.floor(t * 30), 72, 118); ctx.restore();\n}\nfunction progress(ctx, w, h, t, d) { ctx.fillStyle = 'rgba(255,255,255,.14)'; rr(ctx, 120, h - 74, w - 240, 12, 6); ctx.fill(); const g = ctx.createLinearGradient(120, 0, w - 120, 0); g.addColorStop(0, '#00D4FF'); g.addColorStop(.55, '#7BD88F'); g.addColorStop(1, '#F2AA4C'); ctx.fillStyle = g; rr(ctx, 120, h - 74, (w - 240) * clamp(t / 16), 12, 6); ctx.fill(); }\n\nfunction render(t, context) {\n  const p = easeOut(context.progress ?? 0); const ctx = context.ctx; const w = context.width; const h = context.height; background(ctx, w, h, t);\n  ctx.save(); ctx.globalAlpha = p; ctx.textAlign = 'center'; ctx.fillStyle = '#FFFFFF'; ctx.font = '900 80px Arial, sans-serif'; ctx.shadowColor = '#FFFFFF'; ctx.shadowBlur = 24; ctx.fillText('Morning standup ready.', w / 2, 390 + (1 - p) * 36); ctx.shadowBlur = 0; ctx.fillStyle = '#BDEFFF'; ctx.font = '30px Arial, sans-serif'; ctx.fillText('A business report video rendered from one runtime JSON.', w / 2, 456);\n  ctx.fillStyle = '#7BD88F'; rr(ctx, w / 2 - 175, 548, 350, 76, 24); ctx.fill(); ctx.fillStyle = '#07111F'; ctx.font = '900 24px Arial, sans-serif'; ctx.fillText('render the ops update', w / 2, 596);\n  const stats = [['orders', '128K'], ['risk saved', '$42K'], ['stores', '94']]; for (let i = 0; i < stats.length; i++) { const x = w / 2 - 360 + i * 360; ctx.fillStyle = 'rgba(255,255,255,.08)'; rr(ctx, x - 120, 688, 240, 104, 24); ctx.fill(); ctx.strokeStyle = i === 1 ? '#F2AA4C' : '#00D4FF'; ctx.stroke(); ctx.fillStyle = '#FFFFFF'; ctx.font = '900 30px Arial, sans-serif'; ctx.fillText(stats[i][1], x, 730); ctx.fillStyle = '#A9BDD2'; ctx.font = '20px Arial, sans-serif'; ctx.fillText(stats[i][0], x, 766); }\n  ctx.restore(); hud(ctx, w, h, t, 'final report'); progress(ctx, w, h, t, 16);\n}"
      }
    ]
  },
  "scene": {
    "root": {
      "id": "root",
      "type": "root",
      "children": []
    }
  }
}
